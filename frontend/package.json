{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync-docs": "mkdir -p public/docs/assets && cp ../docs/*.md public/docs/ && cp ../docs/assets/*.gif public/docs/assets/ 2>/dev/null || true",
    "dev": "npm run sync-docs && vite",
    "build": "npm run sync-docs && vue-tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "marked": "^18.0.5",
    "vue": "^3.5.24"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@vitejs/plugin-vue": "^6.0.1",
    "@vue/tsconfig": "^0.8.1",
    "typescript": "~5.9.3",
    "vite": "^7.2.4",
    "vue-tsc": "^3.1.4"
  }
}
